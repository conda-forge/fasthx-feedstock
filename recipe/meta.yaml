{% set name = "fasthx" %}
{% set version = "0.2407.0" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/fasthx-{{ version }}.tar.gz
  sha256: a5c30b64555b6ee9c64f55f4c765effe8bc9dd9ff86c8390e86c448933feee6c

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python >=3.10.0,<4.0.0
    - poetry-core
    - pip
  run:
    - python >=3.10.0,<4.0.0
    - fastapi >=0.100.0

test:
  imports:
    - fasthx
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://github.com/volfpeter/fasthx
  doc_url: https://volfpeter.github.io/fasthx/
  summary: FastAPI data APIs with HTMX support.
  license: MIT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - thewchan
